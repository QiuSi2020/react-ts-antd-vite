import styled, {centerBox} from '@/assets/style'

const em = '10px'

// 绑定一个类名，方便查找
// const 名称 尽量以Div结尾(规范)
export const PageName_Div = styled.div.attrs({className: 'PageName_Css'})`
    /* ${em}
    ${centerBox} */
`
